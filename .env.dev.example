# Development environment — copy to .env.dev for local overrides
# Used by docker-compose.dev.yml
# Most dev vars are already hardcoded in docker-compose.dev.yml; add overrides here if needed.

# Override ports if defaults (8080/8081) conflict with something local
GALLERY_PUBLIC_PORT=8080
GALLERY_PRIVATE_PORT=8081
